DB_HOST=localhost
DB_PORT=5432
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña
DB_NAME=simulador_preguntas
# DATABASE_URL=postgres://user:pass@host:port/db  # Railway (tiene prioridad sobre las vars anteriores)

PORT=3001

# true → carga todas las preguntas del bloque en orden, sin límite ni aleatorio
DEBUG=false

# Clave larga y aleatoria para firmar JWT
JWT_SECRET=cambia_esto_por_una_clave_larga_y_aleatoria
